name: custom_charts
version: 0.12.0
description: Material Design charting library for flutter.
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.14.5
  intl: any
  logging: ^1.0.2
  meta: ^1.1.1

  charts_common:
    path: ../charts_common/

dev_dependencies:
  mockito: ^5.0.0
  build_runner: any
  flutter_test:
    sdk: flutter
  test: ^1.3.0
